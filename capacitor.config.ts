import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'APP_TESTES_CODIGO',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
